#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int centimetros(float x)
{
	float metros,cent;
	cent = (metros*100);
}
int main() 
{
	float metros;
	printf("Digite um numero em metros para ser convertido em centimetros");
	scanf("%f",&metros);
	centimetros(metros);
	printf("o valor em centimetro eh:%f",cent);
	return 0;
}
